{
  "name": "@orbit/example-notion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=23"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@orbit/app": "*"
  },
  "devDependencies": {
    "@orbit/testing": "*",
    "typescript": "*",
    "vitest": "*",
    "wrangler": "^3.90.0",
    "@cloudflare/workers-types": "*"
  }
}
